% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{phase_plane_2D}
\alias{phase_plane_2D}
\title{Plot two-dimensional phase plane}
\usage{
phase_plane_2D(
  observations,
  names,
  values,
  x,
  y,
  model,
  predictions = NULL,
  vectors = FALSE,
  grid_interval = 10,
  vector_color = "#529ee0",
  mag_scale = FALSE,
  covariates = NULL,
  covariates_values = NULL
)
}
\arguments{
\item{observations}{A long-format data frame of observed values over time.}

\item{names}{The column in \code{observations} containing the identifier for the
response variable (e.g., series or species name).}

\item{values}{The column in \code{observations} containing the values for the
response variable (e.g., species abundances).}

\item{x}{The state variable to be displayed on the x-axis.}

\item{y}{The state variable to be displayed on the y-axis.}

\item{model}{A trained model that will be passed to the \code{get_right_hand_side()}
function, which yields the vector field. Optional.}

\item{predictions}{A long-format data frame of predicted values over time, with
column names matching the column names of \code{observations}. Optional.}

\item{vectors}{Boolean to display the vector field or not (default).}

\item{grid_interval}{The number of intervals separating grid points to display in each direction.
Default is 10 in both the horizontal and vertical directions, yielding 121 grid points.}

\item{vector_color}{The color of the vectors.}

\item{mag_scale}{Boolean to display the vector magnitude on the original scale
or a standard scale (default).}

\item{covariates}{A data frame of covariate data. Optional.}

\item{covariates_values}{The numeric column in the covariate data frame used to display
facet panels. The column is cut into four intervals and the median value is
displayed as the panel label.}
}
\value{
A \code{ggplot2} figure showing the phase plane of observations and model
predictions. Can be adjusted with additional \code{ggplot2} functions.
}
\description{
\code{phase_plane_2D()} creates a two-dimensional phase plane showing how the two
state variables co-occur at each time point. Observations are plotted as points
and model predictions as lines.
}
\details{
As with \code{ggplot2} aesthetics, do not enclose the column names in quotes.
}
\examples{
df <- data.frame("time" = rep(seq(1,100),4),
                 "species" = c(rep("A",100),rep("B",100),rep("C",100),rep("D",100)),
                 "abundance" = c(rpois(200,40),rpois(200,10)))
phase_plane_2D(observations = df, names = species, values = abundance,
               x = A, y = C) + ggplot2::labs(x =  "Species A", y = "Species C")
}
