% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_analysis.R
\name{get_right_hand_side}
\alias{get_right_hand_side}
\title{Get the right-hand side of the UDE model}
\usage{
get_right_hand_side(model)
}
\arguments{
\item{model}{The UDE model to evaluate.}
}
\value{
A function that returns the value of the ODE model. The function will
require different arguments depending on the UDE model. If the UDE does not have
covariates, then the right-hand side function will take two arguments \verb{(t,u)}, where
\code{u} is a vector with the value of the state variables and \code{t} is time. If covariates
are used, then the arguments are \verb{(t,u,X)}, where \code{X} is a vector of the covariates.
}
\description{
\code{get_right_hand_side()} creates an R function that returns the right-hand side
of the UDE model given the time \code{t}, state \code{u}, and covariates \code{X} if included
in the model.
}
\examples{
NODE_model <- NODE(data=data)
train(NODE_model)
rhs <- get_right_hand_side(NODE_model)
u <- c(1.1,2)
t <- 0.0
rhs(u,t) # evaluates
# Simulate solutions of UDE model with deSolve
library(deSolve)
ode(y=as.u, times = seq(0,10.0,0.2), parms = c(),
   func = function(t,u,pars){list(rhs(t,u))}, method = "lsoda")
}
